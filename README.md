# BotPresent

Telegram-бот на Bun, TypeScript и [grammY](https://grammy.dev/), работающий локально без Docker:

- **📊 Презентации** — создаёт, обновляет и хранит персональные `index.html` и PDF по многоэкранному шаблону из `TestSite`, показывает прогресс 0–100% и наполняет страницы публичными данными конкретного сайта.
- **🎯 Лидогенерация** — ищет до 60 сайтов по точным и расширенным запросам, глубоко анализирует нишу, регион и внутренние страницы, ранжирует точные/частичные/похожие компании и выгружает CSV с владельцами, руководителями, менеджерами и контактами.

После лидогенерации кнопка **«Презентации для всех лидов»** последовательно создаёт HTML- и PDF-версии для каждого найденного сайта.

Проект работает без платных API и ключей AI. Для запуска нужен только токен Telegram-бота и исходящий доступ в интернет.

## Быстрый старт

### 1. Создайте Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather).
2. Выполните `/newbot` и следуйте инструкциям.
3. Сохраните полученный токен.

### 2. Настройте проект

Установите [Bun](https://bun.sh/), затем выполните:

```bash
git clone https://github.com/ASGdevelor/BotPresent.git
cd BotPresent
bun install
```

Скопируйте `.env.example` в `.env` и укажите токен:

```env
BOT_TOKEN=ваш_токен_от_BotFather
```

Для совместимости также распознаётся переменная `TOKEN`, но в новых конфигурациях рекомендуется `BOT_TOKEN`.

### 3. Запустите

```bash
bun run start
```

Режим разработки с автоматическим перезапуском:

```bash
bun run dev
```

## Команды

| Команда | Назначение |
| --- | --- |
| `/start` | Запустить бота и показать главное меню |
| `/menu` | Повторно показать главное меню |
| `/cancel` | Отменить текущее действие |
| `/history` | Выгрузить последние 100 сообщений в `.md` |
| `/clear_history` | Полностью удалить свою историю в текущем чате |

## Проверки

```bash
bun run typecheck
bun test
bun run check
```

## Структура проекта

```text
src/
├── index.ts                  # запуск и команды Telegram
├── bot.ts                    # маршрутизация сообщений
├── config.ts                 # проверка переменных окружения
├── keyboard.ts               # главное меню
├── lead-form.ts              # типизированная пятишаговая анкета
├── services/
│   ├── presentation.ts       # генерация и хранение HTML/PDF-презентаций
│   ├── lead-generation.ts    # поиск и анализ компаний
│   ├── message-history.ts    # постоянная SQLite-история
│   └── public-web.ts         # безопасная загрузка публичных сайтов
├── types/lead.ts             # типы анкеты, компаний и контактов
└── utils.ts                  # обработка текста и имён файлов
tests/                        # unit-тесты
docs/ACTIONS.md               # действия пользователя и сценарии
docs/LEAD_GENERATION.md       # устройство лидогенерации
docs/HISTORY.md               # хранение и удаление истории
docs/CHANGELOG.md             # журнал изменений
docs/DEPLOYMENT.md            # развёртывание
```

## Как работает бот

Состояние выбранного действия хранится в памяти процесса. Для лидогенерации бот последовательно запрашивает: кому продавать, кого искать, где искать, что предложить и кого исключить. Все пять ответов входят в поисковые запросы и фильтрацию. Бот запрашивает Google и Bing, при недоступности выдачи использует резервный поиск, проверяет до 250 уникальных официальных сайтов и формирует Word-совместимый отчёт. В него попадают только подтверждённые содержимым сайта точные и частичные совпадения; случайные и похожие без отраслевых признаков исключаются. Telegram, руководители/менеджеры и другие контакты выводятся только когда найдены на сайте, вместе со страницей-источником; пустые контактные поля не заполняются заглушками.

Для контакта указываются предполагаемая роль, подпись на сайте и страница-источник. Роль определяется эвристически по открытому тексту и требует ручной проверки. Приватные сетевые адреса, invite-ссылки и аккаунты ботов исключаются.

Входящие сообщения, ответы и события документов сохраняются в локальной SQLite не более 90 дней и 200 записей на пользователя в чате. Пользователь может выгрузить или удалить свою историю. База данных и её служебные файлы исключены из Git.

Презентации сохраняются в `data/presentations/`. Бот отправляет автономный редактируемый `index.html` и, если в системе найден Microsoft Edge или Google Chrome, PDF на одном непрерывном листе. PDF создаётся в фиксированной десктопной области 1280 px, поэтому на любом устройстве сохраняет размеры, отступы и взаимное положение элементов HTML; PDF-просмотрщик только масштабирует целый лист под экран. Сначала бот извлекает продуктовые направления, изображения и фирменную палитру, затем через Bing, DuckDuckGo и Google News исследует рынок, аудиторию и конкурентное поле. Источники сохраняются во внутренних данных для проверки, но презентация показывает не список ссылок, а продающий бизнес-разбор: цепляющий заголовок, сильные и слабые стороны бренда, сегменты аудитории, конкурентов, предложение AI-блогеров и 90-дневный план запуска. В HTML столбиковые и круговые диаграммы работают на встроенном Chart.js 4.5.1 без CDN; PDF печатает ту же экранную версию после полной загрузки диаграмм, шрифтов и изображений. Логотипы, продуктовые изображения и тематические GIF AI-блогеров встраиваются как `data:` URI.

После ввода сайта бот спрашивает, продаются ли компании AI-блогеры. Для ответа «да» автоматически выбирается отдельный `Generic/index3.html`: персональный разбор, ниша покупателей, сравнение с живыми блогерами, экономика, три роли AI-персонажей, первая серия, блок запуска в СНГ, рыночные доказательства и CTA пилота. Под бренд заново формируются заголовок, диагностика, аудитория, конкуренты, темы и графики. Прежний процесс генерации презентации сохранён: те же три `data:image/gif` встраиваются в HTML и попадают в PDF. Их содержимое заменено на полноцветные talking-head AI-видео 256×454 по 15 кадров без процедурной 16-цветной пиксельной отрисовки. Для каждого ID презентации сохраняется собственный seed: функция подбирает отраслевую палитру и меняет цвета одежды, вывесок, деталей интерьера и фона с учётом темы бизнеса и фирменных цветов. При повторном редактировании того же ID варианты остаются прежними. В обычном режиме используется улучшенный `Generic/index.html`: AI-формулировки и GIF не выводятся, а продуктовые фото берутся с анализируемого сайта. Оба шаблона содержат семь интерактивных Chart.js-графиков и автономные SVG-fallback.

Кнопка **🛠 Расширенное изменение** открывает пошаговый редактор: пользователь выбирает презентацию по ID, затем один из восьми разделов и локально меняет его заголовок, основной текст или фотографию. Отдельное поле либо все правки раздела можно очистить; настройки сохраняются в `presentation.json` только выбранной презентации и применяются одинаково к HTML и PDF.

## Развёртывание

Инструкции для локального запуска и systemd находятся в [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md).

## Документация действий

Пошаговое описание кнопок, команд, результатов, ошибок и проверочного сценария находится в [docs/ACTIONS.md](docs/ACTIONS.md).

Техническое описание анкеты, поиска, фильтрации, типов и обработки ошибок находится в [docs/LEAD_GENERATION.md](docs/LEAD_GENERATION.md). Правила истории описаны в [docs/HISTORY.md](docs/HISTORY.md). История изменений ведётся в [docs/CHANGELOG.md](docs/CHANGELOG.md).

## Лицензия

[MIT](LICENSE)
